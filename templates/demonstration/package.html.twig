{% extends 'layout.html.twig' %}

{% block content %}
    <div class="row justify-content-center">
        <div class="col">
{#            <h2>Packages</h2>#}
        </div>
    </div>
    <div class="row">
{#        <span>{{ package.id }}.{{ package.fid }}.{{ package.currentPlace }}</span>#}
    </div>

{% endblock %}
